# 06 — ZSCALER BRIDGE STORIES (REAL VICKY)

Translate **your** work into panel language.

| You actually did | Say for Zscaler Staff |
|------------------|------------------------|
| Freelance: PRD → complete e-commerce/SaaS | Learned end-to-end product delivery under client ambiguity |
| Joined startup and built SaaS WMS 0→1 | Primary Staff story: architecture + implementation + production lifecycle ownership |
| JWT + RBAC + ABAC + RLS | Defense-in-depth multi-tenant authz |
| nginx reverse proxy | Origin proxy layer: TLS, routing, upstream |
| Cloudflare DNS + proxy | Edge DNS/TLS/CDN in front of origin |
| GitHub Actions → VPS/AWS | Production delivery + rollback thinking |
| Redis jobs off API | Keep data/request path clean |
| Rust locking P99 800→120 | Hot-path performance + concurrency correctness |
| Idempotent APIs | Safe retries / at-least-once |
| Default deny missing authz | Fail-closed instinct for security products |

## Three hero titles

1. **Startup WMS zero-to-one owner** — idea/PRD to long-lived production SaaS  
2. **Tenant isolation stack** — JWT/RBAC/ABAC/RLS + tests  
3. **Edge to origin path** — Cloudflare → nginx → app → Postgres  

## Golden bridge sentence

> “Freelancing taught me how to deliver complete products. At the startup, I used that skill to build and own a multi-tenant SaaS WMS from zero to one. Zscaler is a much larger security-cloud environment, but I’m bringing the same ownership of architecture, isolation, delivery, operations, and hot-path Rust — and I’m ready to deepen policy and security at your scale.”
